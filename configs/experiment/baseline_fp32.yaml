# Override: entrenamiento FP32 baseline con ArcFace
# Hereda todos los valores de configs/config.yaml

training:
  epochs: 30
  lr: 0.01
  checkpoint_dir: "outputs/checkpoints/baseline_fp32"

model:
  arcface:
    margin: 0.5
    scale: 30.0